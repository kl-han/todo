name: quadrant_application
description: Application commands, queries, and services for Quadrant Todo.
version: 1.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  quadrant_domain: ^1.0.0
  timezone: ^0.10.0

dev_dependencies:
  test: ^1.25.0
