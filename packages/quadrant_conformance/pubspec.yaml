name: quadrant_conformance
description: >-
  Backend contract suite: every REST behavior must pass unchanged against
  the embedded backend isolate and the standalone server process.
version: 1.3.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  http: ^1.2.0
  quadrant_api_client: ^1.0.0
  quadrant_backend_host: ^1.0.0
  test: ^1.25.0

dev_dependencies:
  lints: ^6.0.0
